// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// Deployed as a GitHub Pages user site, served from the root: no `base` needed.
export default defineConfig({
	site: 'https://lautarovera.github.io',
});
